import React from "react";
import BlogContent from "./components/BlogContent";

/**
 * BlogPage Component
 * 
 * Blog page content (Navbar and Footer are in LandingLayout)
 */
export default function BlogPage() {
  return <BlogContent />;
}